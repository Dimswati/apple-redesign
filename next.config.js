/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['rb.gy', 'www.gravatar.com', 'raw.githubusercontent.com', 'cdn.sanity.io']
    }
}

module.exports = nextConfig
